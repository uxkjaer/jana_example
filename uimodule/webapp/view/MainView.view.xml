 <mvc:View controllerName="com.myorg.jana.controller.MainView"
  displayBlock="true"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc">
  <App id="idAppControl" >
    <pages>
      <Page title="{i18n>title}">
        <content>
          <Text text="{local>/userId}" />
          <Text text="{local>/title}" />
          <Text text="{local>/body}" />
        </content>
      </Page>
    </pages>
  </App>
</mvc:View>